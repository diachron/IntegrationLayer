INSERT INTO <changesOntology> {
?dmfft a co:Detach_Measure_From_FT;
    co:dmfft_p1 ?m;
	co:dmfft_p2 ?ft;
    co:old_version ?v1;
    co:new_version ?v2.
}
WHERE {
GRAPH <deletedLowLevelChanges> { 
?ft diachron:hasMeasure ?m.
}
#
BIND(IRI('v1') as ?v1).
BIND(IRI('v2') as ?v2).
BIND(concat("dmfft",str(?m), str(?ft), str(?v1), str(?v2)) as ?url) .
#
BIND(IRI(CONCAT('http://dmfft/',SHA1(?url))) AS ?dmfft).
}

#DELETE WHERE {
#GRAPH <deletedLowLevelChanges> { 
#?ft diachron:hasMeasure ?m.
#}
#}